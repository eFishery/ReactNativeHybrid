ext {
    // core
    def kotlin_version = '1.3.41'
    def ktx_version = '1.0.2'
    def appcompat_version = '1.0.2'
    def junit_version = '4.1.2'
    def dagger_version = '2.19'
    def room_version = "2.1.0"
    def reactnative_version = '0.58.6'

    coreDependencies = [
            kotlin   : "org.jetbrains.kotlin:kotlin-stdlib-jdk7:${kotlin_version}",
            coreKtx  : "androidx.core:core-ktx:${ktx_version}",
            appCompat: "androidx.appcompat:appcompat:${appcompat_version}",
            dagger   : "com.google.dagger:dagger:${dagger_version}"
    ]

    // room
    roomDependencies = [
            runtime: "androidx.room:room-runtime:${room_version}",
            ktx    : "androidx.room:room-ktx:${room_version}"
    ]

    // kapt
    kaptDependencies = [
            roomCompiler  : "androidx.room:room-compiler:${room_version}",
            daggerCompiler: "com.google.dagger:dagger-compiler:${dagger_version}",
    ]

    // reactNative
    reactNativeDependencies = [
            reactNatvie: "com.facebook.react:react-native:${reactnative_version}"
    ]


    // test
    testDependencies = [
            room : "androidx.room:room-testing:${room_version}",
            junit: "junit:junit:${junit_version}"
    ]

    // androidTest
    androidTestDependecies = [
            runner  : 'androidx.test:runner:1.2.0',
            espresso: 'androidx.test.espresso:espresso-core:3.2.0'
    ]
}